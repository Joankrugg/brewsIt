<%= simple_form_for @beer do |f| %>
  <%= f.input :photo %>
  <%= f.input :name %>
  <%= f.input :level %>
  <%= f.association :yeast %>
  <%= f.association :color %>
  <%= f.button :submit %>

<% end %>
