<%= simple_form_for @beer do |f| %>
  <%= f.input :photo %>
  <%= f.input :name %>
  <%= f.input :level %>
  <%= f.association :yeast %>
  <%= f.association :color %>
  <%= f.association :taste %>
  <%= f.association :textures, as: :check_boxes %>
  <%= f.button :submit %>

<% end %>
