<%= turbo_frame_tag "beers" do %>
  <%= render beers %>
  <div class="apple_pagination">
    <%= will_paginate beers, inner_window: 2, outer_window: 2, previous_label: 'Précédent', next_label: 'Suivant' %>
  </div>
<% end %>
